# The API is developed using node.js and express.js, the system needs to have node installed.

# Edit default.json file with your MongoDB connection string and baseUrl

# Use following command for Running
npm start
